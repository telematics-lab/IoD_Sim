From b3f5357d37eb6bae0f37d1c5b4448bc7a4ecab08 Mon Sep 17 00:00:00 2001
From: Domingo Dirutigliano <me@domy.sh>
Date: Mon, 6 Jul 2026 15:56:33 +0200
Subject: [PATCH 2/5] Support for multiple NR devices on the same node

---
 helper/nr-helper.cc      | 121 ++++++++++++++++++++++++++++++++++++---
 helper/nr-helper.h       |  24 +++++++-
 model/nr-spectrum-phy.cc |  21 +++++++
 model/nr-spectrum-phy.h  |   2 +
 model/nr-ue-phy.cc       |  44 ++++++++++++++
 model/nr-ue-phy.h        |  16 ++++++
 6 files changed, 220 insertions(+), 8 deletions(-)

diff --git a/helper/nr-helper.cc b/helper/nr-helper.cc
index 84346766..65c18841 100644
--- a/helper/nr-helper.cc
+++ b/helper/nr-helper.cc
@@ -24,6 +24,9 @@
 #include "ns3/internet-stack-helper.h"
 #include "ns3/multi-model-spectrum-channel.h"
 #include "ns3/names.h"
+#include "ns3/net-device-container.h"
+#include "ns3/node-container.h"
+#include "ns3/node-list.h"
 #include "ns3/nr-ch-access-manager.h"
 #include "ns3/nr-chunk-processor.h"
 #include "ns3/nr-epc-gnb-application.h"
@@ -64,7 +67,11 @@ NS_LOG_COMPONENT_DEFINE("NrHelper");
 
 NS_OBJECT_ENSURE_REGISTERED(NrHelper);
 
+uint16_t NrHelper::m_cellIdCounter = 1;
+uint64_t NrHelper::m_imsiCounter = 1;
+
 NrHelper::NrHelper()
+    : m_useIdealRrc(true)
 {
     NS_LOG_FUNCTION(this);
     m_channelFactory.SetTypeId(MultiModelSpectrumChannel::GetTypeId());
@@ -195,12 +202,15 @@ uint32_t
 NrHelper::GetNumberBwp(const Ptr<const NetDevice>& gnbDevice)
 {
     NS_LOG_FUNCTION(gnbDevice);
-    Ptr<const NrGnbNetDevice> netDevice = DynamicCast<const NrGnbNetDevice>(gnbDevice);
-    if (netDevice == nullptr)
+    if (Ptr<const NrGnbNetDevice> netDevice = DynamicCast<const NrGnbNetDevice>(gnbDevice))
+    {
+        return netDevice->GetCcMapSize();
+    }
+    if (Ptr<const NrUeNetDevice> netDevice = DynamicCast<const NrUeNetDevice>(gnbDevice))
     {
-        return 0;
+        return netDevice->GetCcMapSize();
     }
-    return netDevice->GetCcMapSize();
+    return 0;
 }
 
 Ptr<NrGnbPhy>
@@ -632,6 +642,10 @@ NrHelper::InstallSingleUeDevice(
     dev->SetAttribute("NrUeComponentCarrierManager", PointerValue(ccmUe));
     dev->SetAttribute("Imsi", UintegerValue(n->GetId()));
 
+    rrc->TraceConnectWithoutContext(
+        "HandoverStart",
+        MakeBoundCallback(&NrHelper::OnHandoverStartTrampoline, this, dev));
+
     n->AddDevice(dev);
 
     if (m_nrEpcHelper != nullptr)
@@ -963,7 +977,16 @@ NrHelper::InstallSingleGnbDevice(
     {
         NS_LOG_INFO("adding this gNB to the EPC");
         m_nrEpcHelper->AddGnb(n, dev, cellId);
-        Ptr<NrEpcGnbApplication> gnbApp = n->GetApplication(0)->GetObject<NrEpcGnbApplication>();
+        Ptr<NrEpcGnbApplication> gnbApp = nullptr;
+        for (uint32_t i = 0; i < n->GetNApplications(); ++i)
+        {
+            Ptr<NrEpcGnbApplication> app = n->GetApplication(i)->GetObject<NrEpcGnbApplication>();
+            if (app && app->GetCellId() == cellId)
+            {
+                gnbApp = app;
+                break;
+            }
+        }
         NS_ASSERT_MSG(gnbApp != nullptr, "cannot retrieve NrEpcGnbApplication");
 
         // S1 SAPs
@@ -972,7 +995,7 @@ NrHelper::InstallSingleGnbDevice(
 
         // X2 SAPs
         Ptr<NrEpcX2> x2 = n->GetObject<NrEpcX2>();
-        x2->SetEpcX2SapUser(rrc->GetEpcX2SapUser());
+        x2->SetEpcX2SapUser(cellId, rrc->GetEpcX2SapUser());
         rrc->SetEpcX2SapProvider(x2->GetEpcX2SapProvider());
     }
     return dev;
@@ -1024,6 +1047,31 @@ NrHelper::AddX2Interface(Ptr<Node> gnbNode1, Ptr<Node> gnbNode2)
     m_nrEpcHelper->AddX2Interface(gnbNode1, gnbNode2);
 }
 
+void
+NrHelper::AddX2Interface(NetDeviceContainer gnbDevs)
+{
+    NS_LOG_FUNCTION(this);
+
+    NS_ASSERT_MSG(m_nrEpcHelper, "X2 interfaces cannot be set up when the EPC is not used");
+
+    for (auto i = gnbDevs.Begin(); i != gnbDevs.End(); ++i)
+    {
+        for (auto j = i + 1; j != gnbDevs.End(); ++j)
+        {
+            AddX2Interface(*i, *j);
+        }
+    }
+}
+
+void
+NrHelper::AddX2Interface(Ptr<NetDevice> gnbDev1, Ptr<NetDevice> gnbDev2)
+{
+    NS_LOG_FUNCTION(this);
+    NS_LOG_INFO("setting up the X2 interface");
+
+    m_nrEpcHelper->AddX2Interface(gnbDev1, gnbDev2);
+}
+
 void
 NrHelper::HandoverRequest(Time hoTime,
                           Ptr<NetDevice> ueDev,
@@ -1270,7 +1318,7 @@ NrHelper::DoAttachToGnb(const Ptr<NetDevice>& ueDevice, const Ptr<NetDevice>& gn
         }
     }
 
-    if (m_nrEpcHelper)
+    if (m_nrEpcHelper && ueNetDev->GetTargetGnb() == nullptr)
     {
         // activate default QoS flow
         m_nrEpcHelper->ActivateQosFlow(ueDevice,
@@ -1291,6 +1339,65 @@ NrHelper::DoAttachToGnb(const Ptr<NetDevice>& ueDevice, const Ptr<NetDevice>& gn
     }
 }
 
+void
+NrHelper::OnHandoverStartTrampoline(NrHelper* helper,
+                                    Ptr<NetDevice> ueDevice,
+                                    const uint64_t imsi,
+                                    uint16_t cellId,
+                                    uint16_t rnti,
+                                    uint16_t targetCellId)
+{
+    helper->OnHandoverStart(ueDevice, imsi, cellId, rnti, targetCellId);
+}
+
+void
+NrHelper::OnHandoverStart(Ptr<NetDevice> ueDevice,
+                          const uint64_t imsi,
+                          uint16_t cellId,
+                          uint16_t rnti,
+                          uint16_t targetCellId)
+{
+    NS_LOG_FUNCTION(this << ueDevice << imsi << cellId << rnti << targetCellId);
+
+    if (m_beamformingHelper)
+    {
+        Ptr<NrGnbNetDevice> sourceGnb;
+        Ptr<NrGnbNetDevice> targetGnb;
+
+        for (auto it = NodeList::Begin(); it != NodeList::End(); ++it)
+        {
+            Ptr<Node> node = *it;
+            for (uint32_t i = 0; i < node->GetNDevices(); ++i)
+            {
+                Ptr<NrGnbNetDevice> gnb = DynamicCast<NrGnbNetDevice>(node->GetDevice(i));
+                if (gnb)
+                {
+                    if (gnb->GetCellId() == cellId)
+                    {
+                        sourceGnb = gnb;
+                    }
+                    if (gnb->GetCellId() == targetCellId)
+                    {
+                        targetGnb = gnb;
+                    }
+                }
+            }
+            if (sourceGnb && targetGnb)
+            {
+                break;
+            }
+        }
+
+        if (targetGnb != sourceGnb)
+        {
+            m_beamformingHelper->RemoveBeamformingTask(sourceGnb,
+                                                       DynamicCast<NrUeNetDevice>(ueDevice));
+
+            AttachToGnb(ueDevice, targetGnb);
+        }
+    }
+}
+
 uint8_t
 NrHelper::ActivateDedicatedQosFlow(NetDeviceContainer ueDevices,
                                    NrQosFlow flow,
diff --git a/helper/nr-helper.h b/helper/nr-helper.h
index 02f7f3fe..37b30575 100644
--- a/helper/nr-helper.h
+++ b/helper/nr-helper.h
@@ -328,6 +328,25 @@ class NR_EXPORT NrHelper : public Object
      */
     void AttachToGnb(const Ptr<NetDevice>& ueDevice, const Ptr<NetDevice>& gnbDevice);
 
+    /**
+     * @brief Trampoline to connect HandoverStart trace
+     */
+    static void OnHandoverStartTrampoline(NrHelper* helper,
+                                          Ptr<NetDevice> ueDevice,
+                                          const uint64_t imsi,
+                                          uint16_t cellId,
+                                          uint16_t rnti,
+                                          uint16_t targetCellId);
+
+    /**
+     * @brief Handle HandoverStart trace to update beamforming tasks
+     */
+    void OnHandoverStart(Ptr<NetDevice> ueDevice,
+                         const uint64_t imsi,
+                         uint16_t cellId,
+                         uint16_t rnti,
+                         uint16_t targetCellId);
+
     /**
      * @brief Enables the following traces:
      * Transmitted/Received Control Messages
@@ -1048,6 +1067,8 @@ class NR_EXPORT NrHelper : public Object
     // Handover related functions
     void AddX2Interface(NodeContainer gnbNodes);
     void AddX2Interface(Ptr<Node> gnbNode1, Ptr<Node> gnbNode2);
+    void AddX2Interface(NetDeviceContainer gnbDevs);
+    void AddX2Interface(Ptr<NetDevice> gnbDev1, Ptr<NetDevice> gnbDev2);
 
     std::string GetHandoverAlgorithmType() const;
     void SetHandoverAlgorithmType(std::string type);
@@ -1164,7 +1185,8 @@ class NR_EXPORT NrHelper : public Object
     ObjectFactory m_fhControlFactory;
     ObjectFactory m_initialAttachmentFactory; //!< initial attachment factory
 
-    uint16_t m_cellIdCounter{1}; //!< CellId Counter
+    static uint16_t m_cellIdCounter; //!< CellId Counter
+    static uint64_t m_imsiCounter;   //!< IMSI Counter
 
     Ptr<NrEpcHelper> m_nrEpcHelper{nullptr};                 //!< Ptr to the EPC helper (optional)
     Ptr<BeamformingHelperBase> m_beamformingHelper{nullptr}; //!< Ptr to the beamforming helper
diff --git a/model/nr-spectrum-phy.cc b/model/nr-spectrum-phy.cc
index 132cb707..b58ed692 100644
--- a/model/nr-spectrum-phy.cc
+++ b/model/nr-spectrum-phy.cc
@@ -169,6 +169,12 @@ NrSpectrumPhy::GetTypeId()
                 UintegerValue(1),
                 MakeUintegerAccessor(&NrSpectrumPhy::SetNumPanels, &NrSpectrumPhy::GetNumPanels),
                 MakeUintegerChecker<uint8_t>())
+            .AddAttribute(
+                "SameNodeInterference",
+                "If false, transmissions from devices on the same node do not create interference.",
+                BooleanValue(true),
+                MakeBooleanAccessor(&NrSpectrumPhy::m_sameNodeInterference),
+                MakeBooleanChecker())
             .AddTraceSource("RxPacketTraceGnb",
                             "The no. of packets received and transmitted by the Base Station",
                             MakeTraceSourceAccessor(&NrSpectrumPhy::m_rxPacketTraceGnb),
@@ -660,6 +666,21 @@ NrSpectrumPhy::StartRx(Ptr<SpectrumSignalParameters> params)
         params->spectrumChannelMatrix = CreateSpectrumChannelMatrix(params);
     }
 
+    m_interferenceData->SetSameNodeInterference(m_sameNodeInterference);
+    m_interferenceCtrl->SetSameNodeInterference(m_sameNodeInterference);
+    if (m_interferenceSrs)
+    {
+        m_interferenceSrs->SetSameNodeInterference(m_sameNodeInterference);
+    }
+    if (m_interferenceCsiRs)
+    {
+        m_interferenceCsiRs->SetSameNodeInterference(m_sameNodeInterference);
+    }
+    if (m_interferenceCsiIm)
+    {
+        m_interferenceCsiIm->SetSameNodeInterference(m_sameNodeInterference);
+    }
+
     // pass it to interference calculations regardless of the type (nr or non-nr)
     m_interferenceData->AddSignalMimo(params, duration);
 
diff --git a/model/nr-spectrum-phy.h b/model/nr-spectrum-phy.h
index 0cfa850f..ef8574f1 100644
--- a/model/nr-spectrum-phy.h
+++ b/model/nr-spectrum-phy.h
@@ -798,6 +798,8 @@ class NR_EXPORT NrSpectrumPhy : public SpectrumPhy
         false}; //!< Whether this spectrum phy is configure to work in an unlicensed mode.
                 //   Unlicensed mode additionally to licensed mode allows channel monitoring to
                 //   discover if is busy before transmission.
+    bool m_sameNodeInterference{true}; //!< If false, transmissions from devices on the same node do
+                                       //!< not create interference.
 
     Ptr<SpectrumChannel> m_channel{
         nullptr}; //!< channel is needed to be able to connect listener spectrum phy (AddRx) or to
diff --git a/model/nr-ue-phy.cc b/model/nr-ue-phy.cc
index d1a8e89c..f78dc7e7 100644
--- a/model/nr-ue-phy.cc
+++ b/model/nr-ue-phy.cc
@@ -41,6 +41,7 @@ NS_OBJECT_ENSURE_REGISTERED(NrUePhy);
 NrUePhy::NrUePhy()
 {
     NS_LOG_FUNCTION(this);
+    m_phyEnabled = true;
     m_wbCqiLast = Simulator::Now();
     m_ueCphySapProvider = new MemberNrUeCphySapProvider<NrUePhy>(this);
     m_powerControl = CreateObject<NrUePowerControl>(this);
@@ -325,6 +326,24 @@ NrUePhy::GetTxPower() const
     return m_txPower;
 }
 
+void
+NrUePhy::SetPhyEnabled(bool enable)
+{
+    NS_LOG_FUNCTION(this << enable);
+    m_phyEnabled = enable;
+    if (!m_phyEnabled)
+    {
+        NS_LOG_INFO("PHY disabled, stopping ongoing processes if any (not fully implemented "
+                    "cleanup, just blocking IO)");
+    }
+}
+
+bool
+NrUePhy::IsPhyEnabled() const
+{
+    return m_phyEnabled;
+}
+
 double
 NrUePhy::GetRsrp() const
 {
@@ -614,6 +633,11 @@ void
 NrUePhy::PhyCtrlMessagesReceived(const Ptr<NrControlMessage>& msg)
 {
     NS_LOG_FUNCTION(this);
+    if (!m_phyEnabled)
+    {
+        NS_LOG_DEBUG("PHY disabled, ignoring received control message");
+        return;
+    }
 
     if (msg->GetMessageType() == NrControlMessage::DL_DCI)
     {
@@ -1380,6 +1404,11 @@ NrUePhy::EndVarTti(const std::shared_ptr<DciInfoElementTdma>& dci)
 void
 NrUePhy::PhyDataPacketReceived(const Ptr<Packet>& p)
 {
+    if (!m_phyEnabled)
+    {
+        NS_LOG_DEBUG("PHY disabled, ignoring received data packet");
+        return;
+    }
     Simulator::ScheduleWithContext(m_netDevice->GetNode()->GetId(),
                                    GetTbDecodeLatency(),
                                    &NrUePhySapUser::ReceivePhyPdu,
@@ -1394,6 +1423,12 @@ NrUePhy::SendDataChannels(const Ptr<PacketBurst>& pb,
                           const std::shared_ptr<DciInfoElementTdma>& dci,
                           const Time& duration)
 {
+    if (!m_phyEnabled)
+    {
+        NS_LOG_DEBUG("PHY disabled, skipping data transmission");
+        return;
+    }
+
     if (pb->GetNPackets() > 0)
     {
         NrRadioBearerTag tag;
@@ -1409,6 +1444,15 @@ NrUePhy::SendDataChannels(const Ptr<PacketBurst>& pb,
 void
 NrUePhy::SendCtrlChannels(Time duration)
 {
+    if (!m_phyEnabled)
+    {
+        NS_LOG_DEBUG("PHY disabled, skipping control transmission");
+        // Clear control messages even if not sent, to avoid accumulation?
+        // Or keep them for when enabled?
+        // Clearing is safer to avoid sending outdated control info later.
+        m_ctrlMsgs.clear();
+        return;
+    }
     m_spectrumPhy->StartTxUlControlFrames(m_ctrlMsgs, duration);
     m_ctrlMsgs.clear();
 }
diff --git a/model/nr-ue-phy.h b/model/nr-ue-phy.h
index 00861723..53d4e945 100644
--- a/model/nr-ue-phy.h
+++ b/model/nr-ue-phy.h
@@ -296,6 +296,20 @@ class NR_EXPORT NrUePhy : public NrPhy
      */
     void GenerateDlCqiReport(const SpectrumValue& sinr);
 
+    /**
+     * @brief Set the PHY enabled or disabled.
+     * @param enable true if the PHY should be enabled, false otherwise.
+     *
+     * When disabled, the PHY will not transmit or receive any signals.
+     */
+    void SetPhyEnabled(bool enable);
+
+    /**
+     * @brief Check if the PHY is enabled.
+     * @return true if enabled, false otherwise.
+     */
+    bool IsPhyEnabled() const;
+
     /**
      * @brief Get the current RNTI of the user
      *
@@ -676,6 +690,8 @@ class NR_EXPORT NrUePhy : public NrPhy
      */
     void ReportUeMeasurements();
 
+    bool m_phyEnabled; //!< True if the PHY is enabled (default)
+
     /**
      * @brief A function called by NrHelper to configure in NrUePhy what is the CSI feedback type.
      * @param csiFeedbackType CSI feedback type that is configured by NrHelper
